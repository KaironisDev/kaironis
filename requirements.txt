# Core
python-telegram-bot==21.0

# Database
psycopg2-binary
sqlalchemy==2.0
redis

# Vector DB & Embeddings (optioneel - zie requirements-ml.txt)
# chromadb           # zware dependency, apart installeren
# sentence-transformers  # zware dependency, apart installeren

# SSH tunneling
paramiko

# Trading / Brokers
# hyperliquid-python-sdk  # uncomment when ready
# MetaTrader5             # uncomment when ready (Windows only)

# Utils
pyyaml
python-dotenv
structlog
