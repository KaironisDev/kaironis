# Core
python-telegram-bot==21.0

# Database
psycopg2-binary
sqlalchemy==2.0
redis

# Vector DB & Embeddings (zware ML deps - apart op VPS: requirements-ml.txt)
# chromadb
# sentence-transformers

# SSH tunneling
paramiko

# Trading / Brokers
# hyperliquid-python-sdk  # uncomment when ready
# MetaTrader5             # uncomment when ready (Windows only)

# Utils
pyyaml
python-dotenv
structlog
